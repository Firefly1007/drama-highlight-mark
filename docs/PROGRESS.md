# 当前进度（PROGRESS）

本文件是 V2 唯一的**现状、缺口、未决项与下一步**来源；它不替代目标形态的 [README](../README.md)、产品要求的 [PRD](drama-interaction-v2-PRD.md)、技术决策的 [ADR](drama-interaction-v2-ADR.md)、外部输出契约的 [schema](schema.md) 或模块实现计划的 [plans](plans/README.md)。

最后核对：2026-09-04

## 当前判断

V2 已完成目标架构和文档契约的收敛，并已对齐外部互动模型与证据底座；尚未形成可端到端运行的互动点生成工作流。

项目范围是单集离线互动点标注/生成，并向后端输出 JSON；不包含播放器、前端页面或后端服务本身。

## 当前快照

| 层级 | 状态 | 当前事实 |
| --- | --- | --- |
| 文档体系 | 已收敛 | README、PRD、ADR、schema、plans 与本文件各自承担唯一职责。 |
| 工程壳与配置 | 部分完成 | Python 包、依赖与基础配置已存在；CLI、可恢复执行与端到端导出尚未实现。 |
| 外部输出模型 | 模型层已对齐 | `type` 为五个字符串，`mood` 为必填小写枚举，最终延时投票校验揭晓字段与展示结束关系；最终导出节点尚未实现，届时需以 `exclude_none=True` 序列化可选载荷字段。 |
| 基线证据 | 模型层已对齐 | 已使用 `EvidenceDocument`、精确跨度与从 1 开始的 `T/O/D` 标识；分支私有派生观察使用 `{specialist, query_span, query}` 溯源。 |
| Legacy Adapter | 契约适配已完成 | 已按原始片段跨度构造 `EvidenceDocument`，并消费起始媒体预处理阶段写入状态的 `episode_duration_ms`；不处理媒体或猜测视频路径。 |
| 按需取证 | 合规桩已完成 | `inspect_span` 为无单例、无累积状态的只读桩，返回派生观察列表、可用性、失败原因和审计记录；真实媒体提取尚未接入。 |
| 证据时间线渲染 | 未实现 | `evidence/render.py` 尚未落地。 |
| 五类专家与 LLM 层 | 未实现 | 专家文件目前没有提示词、调用、候选生成或显式弃权实现。 |
| 校验、修复与调度 | 未实现 | 分支内校验、定向修复、锚点换算、全局约束和语义取舍均未落地。 |
| LangGraph、HITL、checkpoint | 未实现 | graph/state/nodes/builder 仍是目标设计，未形成可运行图。 |
| Golden benchmark | 未实现 | 尚未准备人工校正基准集与 V1/V2 对照。 |
| 最终 V2 产物 | 不存在 | 尚未生成可交付给后端的单集互动点 JSON。 |

## 当前简历表述边界

- 可以如实表达：完成 V2 的证据驱动、多专家、确定性约束优先架构设计；整理了五类互动的外部 JSON 契约；已实现其互动模型、证据适配契约与按需取证桩。
- 不应表述为已完成：端到端互动点生成、LangGraph 多专家工作流、检查点/HITL、按需多模态取证、全局调度，或任何未经 benchmark 证实的质量和成本提升。

## 已完成的本次文档整理

- 建立以 README → PRD/ADR/schema → plans → PROGRESS 为入口的文档导航。
- 将产品互动总览、证据表示修订和按需取证规格中的有效内容收敛到权威文档；淘汰草稿由 Git 历史保留。
- 将实现计划改为目标实现口径，并拆分为公共专家骨架加五个专家模块计划。
- 明确 V2 外部契约不以 V1 数字编码为约束；实现差距在本文件中追踪。

## 下一步

1. 实现起始媒体预处理节点，将真实 `episode_duration_ms` 写入工作流状态。
2. 实现证据时间线渲染、五类专家、分支内校验/修复和候选池。
3. 实现渲染、约束、语义调度、最终导出、CLI 与可恢复执行。
4. 建立 golden benchmark，再讨论质量、召回率、成本和调用次数阈值。

## 当前阻塞与风险

- 起始媒体预处理与工作流状态尚未实现；当前适配器只能由调用方显式提供已验证的 `episode_duration_ms`。
- 现有单元测试覆盖模型与适配边界，不能证明真实媒体预处理、专家语义或端到端链路质量。
- 现有工作区含用户的源码与测试改动；文档整理不修改或回退这些改动。

## 未决项

- `inspect_span` 的每分支调用次数上限：先留痕，待 benchmark 数据决定。
- 派生观察是否允许跨运行缓存：当前默认不缓存。
- HITL 如何展示派生观察、其父引用和原始提问。
- 真实视频时长与 V1 时间线的偏差处理：先完成实测，再决定是否需要上游数据修复。
- 各专家提示词的最终措辞，以及跟读金句文本匹配的归一化策略。

## 本轮验证

- 2026-09-04：29 份 README/V2 Markdown 文档的相对链接均可达；已删除草稿无残留引用。
- 2026-09-04：`schema.md` 的 JSON 示例通过标准库解析；示例覆盖五个字符串 `type`，`side_comment.mood` 合法且必填。
- 2026-09-04：ADR 编号连续覆盖 ADR-001 至 ADR-024；22 份模块计划均具备统一的六段结构，五个专家计划文件与源码名称一一对应。
- 2026-09-04：互动、候选、证据、适配器与取证桩的 32 个单元测试通过；`ruff check src tests` 与 `git diff --check` 通过。
