# 短剧互动证据上下文

本上下文统一“原始视频到 Specialist 输入”的领域语言，区分整集事实、分支补证和互动候选，避免把 v1 的高光筛选概念带入 v2。

## Language

**原始视频**：未经互动机会筛选的单集媒体文件，是整集事实观察的来源。
_Avoid_: V1 片段 JSON、 高光视频

**生产视频输入**：生产运行的权威单集输入，来自原始视频文件；剧名和集名由约定的媒体路径解析，文本 JSON 不参与生产证据链。
_Avoid_: 用 V1 文本反推集长、把回归 fixture 当作生产输入

**单集时间轴**：由原始视频的主视频轨时长确定的毫秒范围，是所有台词、观察和互动锚点的共同边界。
_Avoid_: 以音轨尾巴延长集长、多套时间轴

**毫秒时间戳**：从单集起点计算的非负整数偏移，以 `start_ms` / `end_ms` 原样用于证据、Specialist 时间线和 `inspect_span`。
_Avoid_: `HH:MM:SS.mmm` 显示转换、另一套时间表示

**视频批次输入**：以一个目录为起点递归发现的所有原始 `.mp4` 单集集合；起点可以是单剧目录，也可以是包含多部剧的上级目录。每个视频仍是独立的生产视频输入，批次按相对路径的自然顺序串行处理。
_Avoid_: 只扫描直接子文件、把整个目录合并为一集、将集数当字符串排序

**基线证据**：由提取流程按既定采样策略处理整集原始视频后，按时间组织得到的客观文本观察，供所有 Specialist 共享；条目只记录实际观察到的事实，不要求每个毫秒都有证据条目。它不是高光点列表，也不预先判断互动价值。
_Avoid_: 高光证据、候选证据、互动摘要

**派生证据**：某个 Specialist 针对指定时间范围请求补充得到的客观文本观察，只在发起该请求的分支内可见。
_Avoid_: 共享证据、全局补证

**Specialist 证据视图**：一个 Specialist 可读取的基线证据与该分支派生证据的组合。
_Avoid_: 高光列表、候选池

**单集失败**：某一集无法得到可用证据或无法通过证据校验时，记录该集的错误并结束该集处理，不产出互动结果。
_Avoid_: 用空结果伪装成功、静默跳过

**批次继续**：批量处理多个单集时，单集失败只影响当前集；记录错误后按既定的自然顺序开始下一集。
_Avoid_: 一集失败即终止整批

**提取记录**：围绕单集证据提取保留的来源元数据、阶段日志和错误信息；它与通过校验后供 Specialist 使用的基线证据分开。
_Avoid_: 可直接消费的 evidence、最终互动结果

**覆盖状态**：成功单集中，固定视觉采样切片没有对应 `Observation` 时可确定推导出 `covered_empty`，表示该切片按既定采样策略处理成功但没有返回可记录观察。它不表示逐帧检查过该范围，也不表示其中没有发生事件。它可作为不带证据 ID 的状态行提供给 Specialist，无需独立覆盖表。`uncertain` 仅表示确实产生了无法可靠判定的观察。提取器未处理、媒体不可读或提取失败属于单集提取错误，不发布该集基线证据。
_Avoid_: 把所有空结果标成错误或不确定、把处理覆盖解释为内容无遗漏

**证据提取器**：从原始视频产生基线证据的上游处理；它只描述视频中可观察的内容，不判断互动价值或生成互动。
_Avoid_: 高光检测器、互动生成器

**ASR 句段时间戳**：语音识别器为每个台词句段提供的起止时间，是 `TranscriptSegment.start_ms/end_ms` 的直接来源。V2 不需要逐词时间，也不重新生成句段边界。短剧单集识别为零个台词句段属于提取异常，不是可用的空证据。
_Avoid_: 逐词证据、模型猜测时间、二次组句、零句段成功

**媒体切片观察**：OCR 或 VLM 对采样器确定的媒体时间片段给出的客观文本观察；观察跨度继承切片边界。
_Avoid_: 自由毫秒观察、模型自报时间

**屏幕文字**：OCR 从采样帧中识别到的画面内文字，包括硬字幕、标题、标牌和聊天界面等；它可与 ASR 台词重复，但不替代台词证据。
_Avoid_: 只保留非字幕文字、转写台词、与 ASR 语义去重

**视觉观察**：VLM 仅根据当前媒体切片的采样帧描述可见的人物、动作、物体、状态和画面变化；没有画面证据时不使用角色名或剧情设定补全事实，也不把采样帧之间没有显示的过程写成已发生的具体动作。
_Avoid_: 剧情概括、角色身份猜测、帧间动作补全、互动价值判断

**视觉采样切片**：对原始视频按固定、非重叠时间范围划出的处理单元；提取器只观察其中由既定采样策略选出的画面，不等于逐帧检查该范围，也不等于事件边界。
_Avoid_: 高光片段、语义场景、模型自定时间窗

**背景音轨**：从原始视频音轨中去除台词后保留的音乐与音效组合，是基线音频观察的唯一媒体输入。
_Avoid_: 原始混合音轨、人声轨、V1 音频文本

**台词音轨**：从原始视频音轨中分离出的台词与人声组合，是基线语音识别的唯一媒体输入。
_Avoid_: 原始混合音轨、背景音轨

**音频观察**：对背景音轨中可听见的音乐、音效或环境声及其变化所作的客观文本描述，与同跨度的视觉和屏幕文字结果共同属于一个基线观察。无可辨识声音不构成音频观察，但转入或退出静音的变化可以构成。它不解释声音营造的情绪、剧情含义或角色状态。
_Avoid_: 台词转写、气氛判断、剧情暗示、独立音频证据编号
